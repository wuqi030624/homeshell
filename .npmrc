# pnpm 配置
# 使用国内镜像加速
registry=https://registry.npmmirror.com/

# 严格的peer依赖检查（可以避免版本冲突）
strict-peer-dependencies=false

# 自动安装peer依赖
auto-install-peers=true

# 使用符号链接（提高性能）
symlink=true

# shamefully-hoist=false 保持严格的依赖隔离
shamefully-hoist=false
